import { View, Text, Image, Button } from "react-native";

const Navbar = () => {
  return (
    <View>
      <Button>Home</Button>
      <Button>Play</Button>
      <Button>Learn</Button>
      <Button>Settings</Button>
    </View>
  );
};

export default Navbar;
