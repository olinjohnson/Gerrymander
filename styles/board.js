import { StyleSheet } from "react-native";

export const boardStyles = StyleSheet.create({
  rowContainer: {
    display: "flex",
    flexDirection: "row",
  },
  boxSize: {
    width: 75,
    height: 75,
    borderRadius: 10,
    margin: 10,
  },
});
